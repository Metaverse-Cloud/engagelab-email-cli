## Checklist

- [ ] `npm test` passes
- [ ] `npm run packcheck` passes
- [ ] CLI command changes are covered by `tests/command-smoke.test.js`
- [ ] npm version bump is included when publishing is needed
